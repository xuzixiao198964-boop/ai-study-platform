[Unit]
Description=学习指认AI平台 - FastAPI 后端服务
After=network.target postgresql.service redis.service

[Service]
Type=simple
User=aistudy
Group=aistudy
WorkingDirectory=/opt/ai-study-platform
Environment=PATH=/opt/ai-study-platform/venv/bin:/usr/bin
ExecStart=/opt/ai-study-platform/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
