[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not integration"
markers =
    integration: 需真实服务或 INTEGRATION_BASE_URL；显式运行: pytest -m integration
